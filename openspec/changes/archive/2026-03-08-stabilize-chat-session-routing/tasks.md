## 1. Store 行为收敛

- [x] 1.1 为聊天目标定义稳定主智能体过滤规则，并在 `setTargetAgent()` 中统一使用
- [x] 1.2 在 `loadSessions()` 与目标切换路径中实现“最近活跃会话优先”的选择逻辑
- [x] 1.3 在 `handleChatEvent()` 中加入基于 `sessionKey` 的事件隔离

## 2. UI 联动修正

- [x] 2.1 调整 `AppShell` 默认目标同步逻辑，只在有效主智能体集合内选择
- [x] 2.2 修正侧栏选中 agent 与聊天目标同步的边界条件

## 3. 回归验证

- [x] 3.1 为聊天 store 增加会话选择与串台隔离测试
- [x] 3.2 为 `AppShell` 增加默认目标与无效目标回退测试
- [x] 3.3 手动验证切换 agent、切换 session、并发流式消息三种典型路径
